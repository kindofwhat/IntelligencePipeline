plugins {
    id "com.google.cloud.tools.jib" version "0.9.9"
}

dependencies {
    expectedBy  project(':model')
}

jib {
    container {
        mainClass = 'StartPipelineKt'
    }
    to {
        image = 'pipeline:latest'
    }
}